# DataImputation-MLP
This project explores neural network-based techniques for data imputation using Multi-Layer Perceptrons (MLPs). It compares two approaches — training independent MLPs for each feature versus a single conjunct MLP with multiple outputs — to identify the most effective strategy for accurately predicting and filling missing values in datasets.
